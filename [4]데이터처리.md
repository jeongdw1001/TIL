## 1. 데이터(data)

- 개념 : '자료'라는 뜻

- 형식
    - 데이터 형식은 다양
      - 숫자만 해도 분류 방법에 따라 다양하게 부름
      - 진법: 2진수, 8진수, 10진수, 16진수
수체계: 정수, 실수(부동소수점 수)
데이터 형식(data type)에 대한 호칭
자료형
데이터 타입
자료 유형

## 2. 데이터 처리(data processing)

- 정의 : 
  - 데이터를 한 가지 형식에서 다른 형식으로 바꾸는 일
  - 정보를 자료(data)로
  - 자료를 정보로
  
- 주의
  - 데이터 처리는 광범위한 개념
  - 그러므로 데이터 분석은 그 하위 개념

- 목적
  - 데이터를 정제해 정보 생산
  - 정보를 정제해 지식 생산
  - 정보도 지식 생산을 위해 처리되면 데이터로 간주

- 의미 변화
  - 원래의 데이터 처리라는 말은 정보 처리로 대체됨
  - 데이터 처리 시스템 --> 정보 처리 시스템
  - 여기서 말하는 데이터 처리는 저수준 정보 처리를 의미함
  
- 방법
  - 각종 연산자나 함수를 사용해서 처리함

## 3. 변수(variable)

```python
# var에 1을 대입(집어 넣기)한다.
var = 3 # 여기서 var는 변수명, 여기서 1은 값(value)에 해당한다. 
        # 값이 곧 데이터이다.
print(var)     # 변수명을 지정해 값을 나타내게 한다.
print(var + 1) # 변수명에 값을 더할 수 있다.
# 참고: 변수가 가리키는 실제 메모리 주소를 알아보자.
```

```python
id(var)  # 파이썬 내장 함수인 id를 쓰면 그렇게 할 수 있다.
         # 이를 통해 var라는 변수명이 실제로는 9411...로 시작되는 주소값을 담고 있음을 알 수 있다.
         # 즉, 변수명은 사실은 주소값이 나타내는 메모리 장소를 가리키는 지시자(포인터)인 것이다.
# 변수의 값을 변수명으로 지정해도 된다.
```

```python
# 정수 값을 선언하게 되면 해당 타입의 클래스가 호출되어 값을 메모리에 생성된 영역으로 저장한다.
# 3 -> int라는 클래스에 호출 -> __init__ -> 동적할당된 영역에 메모리 로드 -> 참조형 
# -> 모든 클래스의 메소드를 호출

var = 3 # int -> __init__ -> var이라는 int 클래스의 객체를 생성 (__init__= 생성자)했다.
# ex) a=100	#100 정수를 a라는 int 클래스의 객체를 생성했다.
var02 = var # var이 가진 3의 주소를 대입했다.
print(var,var02)
print(id(var))
print(id(var02))
```


>## M/M
1. DS : data segment -> 자료관리영역
2. CS : code segment -> 코드관리영역
3. static -> 정적영역_static(python), class(python), struct, union, 사용자 함수(python)를 선언하게 되면 주소를 관리하며 메모리 정적으로 확보하는 공간
4. heap -> 자유영역공간_ 프로그램이 실행시에 메모리를 확보했다가(동적할당) 소거하는 공간
5. stack -> 고정공간_지역변수 등 고정값들을 가진 공간

>## pvm   
(**************시험문제100%*******************)
1. 실행 영역을 동적으로 할당한다.
2. 파이썬 모듈을 컴파일한다.
3. 프로세스(exe)가 끝나면 동적할당영역에 모든 선언된 객체 등 메모리를 차지하는 리소스들을 소거함
4. 플랫폼에 독립적이다. = 객체 지향 프로그램들
5. 20% 동적공간으로 사용

```python
new_var = var  # var의 값이 new_var로 대입(할당)된다.
print(new_var)
print(new_var + 1)
print(new_var - 1)
```
```python
# 변수에 어떤 값을 추가할 수 있다.
new_var = new_var + 1 # new_var은 앞에서 계산된 값을 담고 있습니다. 건드리지 마세요. 
print(new_var)
```


```python
# 변수끼리 연산을 할 수 있다. 
print(var + new_var)
print(var - new_var)
print(var * new_var)
print(var / new_var)
print(var % new_var)
```
```python
# 변수끼리 연산한 결과를 다른 변수에 넣을 수 있음 
c = var + new_var
d = var - new_var
e = var * new_var
f = var / new_var
g = var % new_var
print(c, d, e, f, g) # print 문에서 쉼표를 쓰면 여러 값을 한 번에 출력할 수 있다.
```



## 4. 데이터 형식(data type), 데이터를 표현하는 방식
- (data type), 자료유형, 데이터 타입 등으로 부름
- 데이터를 프로그램 내에서 표현하는 방식
  - 우리 눈에 보이는 형식
- 데이터를 컴퓨터 내에서 저장하는 방식
  - 우리 눈에 보이지 않는 형식


## 4-1. 숫자
```python
# 정수(integer) 형식 표현
a = 1  # 양수
b = -1 # 음수 
c = 0  # 0 
print(a, b, c)
print(a + b + c)
d = 1 + (-1) + 1 + (-1)
print(d)
```
```python
# 부동 소수점 수(floating point number) 형식 표현
a = 1.2
b = -3.45
c = 0.12345
print(a, b, c)
print(a + b + c)
print(a - b - c)
print(-b)
print(a * b * c)
```
```python
# 정수 표현과 부동 소수점 수(실수) 표현을 섞어 쓰기
a = 1
b = 1.1
c = 0.             # 소수점 아래 숫자가 없다면 생략 가능하다.
print(a, b, c)     # 출력할 때는 생략한 숫자 대신 .0이 출력된다.
print(a + b + c)   # 정수 형식 데이터와 부동소수점 형식 데이터를 연산할 수 있다.
```
```python
#결과
1 1.1 0.0
2.1
```
```python
a = 12.00
b = 1.00
c = 1.00 
print(a + b + c)
```
```python
#결과
14.0
```
```python
# 8진수와 16진수 표현.
a = 0o17      # 8진수. 0O17처럼 o자를 대문자로 써도 되지만 0과 혼동되므로 소문자 사용이 관례.
b = 0xA1      # 16진수. 0XA1처럼 써도 되지만 대문자끼리 구분하기 힘들므로 소문자 사용이 관례.
print(a)      # 8진수 0o17은 10진수로는 15.
print(b)      # 16진수 0xA1은 10진수로는 161.
print(a + b)  # 진법이 달라도 연산을 할 수 있다 
```


## 4.2. 문자열
- 문자열 처리는 자연어 처리 분야에서 필수로 쓰입니다. 
- 그 밖의 인공지능 구현에도 자주 사용됩니다. 꼭 익혀 둡시다.


```python
print(123 + 123)   # 숫자 지정
print("123" + "123") # 문자열 지정 
```

## 4.2.1 문자열 사용해 보기
- 문자들을 따옴표로 엮은 형태로 표현한 게 문자열이다. 

```python
print("안녕하세요?")  # 큰 따옴표로 둘러싸도 문자열이 된다.
print('반갑습니다.')  # 작은 따옴표로 둘러싸도 문자열이 된다.
​
print("우리는 'A반'입니다. '맞죠?'")  # 큰 따옴표 안에 작은 따옴표를 두면 작은 따옴표는 문자로 간주된다.
print('우리는 "A반"인가요? "그렇습니다."') # 작은 따옴표 안에 큰 따옴표를 둬도 마찬가지다.
​
print('오늘은 8월 10일입니다.') # 따옴표 안에 있는 숫자는 문자로 간주된다.
```

- 문자열을 여러 줄에 걸쳐 작성할 때는 
- 3겹 큰 따옴표(""" """)나 3겹 작은 따옴표(''' ''') 사이에 둔다.
- 참고로, 한글로도 변수명을 지을 수 있다.

​
## 4.2.2 문자열 연산
- 문자열끼리 합하고 문자열을 곱할 수 있다.

```python
print("안녕하세요!" + "\tA반입니다.")
# 문자열끼리 더하면 문자열들이 서로 연결된다.
인사말 = "안녕하세요. "
날씨 = "오늘 날씨 좋네요. "
print(인사말 + 날씨)
# 문자열을 곱하면 곱한 횟수만큼 문자열이 반복된다.
print("=" * 107 )  # 주로 줄을 그을 때 문자열을 곱한다.
print(인사말 * 3)  # 언제든지 변수를 다시 불러 와 쓸 수 있다.
print(">>> 안녕하니까 그만 인사하세요!")
print("-" * 107 )  
print(날씨 * 6)
print(">>> 날씨 좋은 줄 알아요. 인사도 적당히!")
print("=" * 107 )
# len() 함수로 문자열 길이 알아내기
print(len(인사말)) # 한글 한 음절이 한 글자로 취급된다.
print(len(날씨))   # 빈 칸도 한 글자로 취급된다.
​
print(len("우리는 A반!"))
```

## 4.2.3 포맷팅(formatting)
- 포맷팅이란 문자열에 서식을 부여하는 일을 말합니다. 
- 다양한 포맷팅 방법이 있지만 여기서는 기초적인 것만 다룹니다.
- 정수%d, 실수%f, 문자열%s ---> '문자열 출력서식' %출력대상

```python
print("정답은 10개입니다.")
​
x = 20
print("정답은 %10d개입니다." %x) # %전체자리수 d
print("정답은 %3.1f개입니다." %x) 
# 전체자리수. 소수점자리수
# 숫자를 직접 대입
print("정답은 %d개입니다." %10) 
```
```python
# 결과
정답은 10개입니다.
정답은         20,        20,        20개입니다.
정답은 20.0개입니다.
```
```python
# 숫자를 변수에 담아서 대입
a = 10
print("정답은 %10d개입니다." %a)   
​
# 변숫값을 바꾸면 출력 내용도 바뀜
a = 20
print("정답은 %10d개입니다." %a)   
​
# 변수에 문자열을 담으면 문자열 출력. 단 %s를 사용.
a = "열"
print("정답은 %-2s개입니다." %a)  
​
# 문자열을 직접 대입
print("정답은 %10s 개입니다." %"열")
```
```python
인삿말 = "안녕하세요?"
print("날씨가 참 좋습니다.   %-2s" %인삿말)
```
```python
# 결과
날씨가 참 좋습니다. 안녕하세요?
```
```python
# 값을 두 개 대입하기
숫자 = 10
문자 = '개'
문자열 = "입니다."
print("정답은 %d%c%s" % (정수, 문자, 문자열))
```
```python
문자열1 = "안녕!"
문자열2 = "잘 가라!"
print("그동안 반가웠어! %s %s " %(문자열1, 문자열2))
```
```python
숫자1 = 1010.1234
숫자2 = 2020.1234
print("%.2f + %.2f = %.2f" %(숫자1, 숫자2, 숫자1+숫자2))
# 10.0 + 20.0 = 30
```
```python
# %s는 어떤 값이든 문자열 형태로 출력한다.
print("파이 값은 %s입니다." % 3.14)
print("파이 값은 %s입니다." % "3.14")
print("정답은 %d%c%s" % (정수, 문자, 문자열))
```
```python

집합1_이름 = 'A'
집합2_이름 = 'B'
print("%c 집합과 %c 집합의 합집합은 무엇인가?" %(집합1_이름, 집합2_이름))
```
```python
# 8진수, 16진수 서식 지정 
print(" 8진수 %5o,       10진법 변환  8진수 %5d" % (0o17, 0o17))
print("16진수 %5x,       10진법 변환 16진수 %5d" % (0xA1, 0xA1))
print(" 8진수 %5o,       16진법 변환  8진수 %5x" % (0o17, 0o17))
```
```python
# 부동 소수점 수 서식 지정
print("파이 값은 %f입니다." % 3.14159)
print("자연대수 값은 %f입니다." % 2.7182818284904523536028)
print("자연대수 값은 %.2f입니다." % 2.7182818284904523536028)
​
# 파이썬 내부 수치 처리 한계 확인하기
# 소수점 이하 16번째 자리부터 오차가 발생하며 근삿값으로 출력한다.
# 이는 반올림 오차가 있는 부동수수점을 사용하기 때문이다.

print("자연대수 값은 %.25f입니다." % 2.7182818284904523536028) # 오차에 주목
​
# [참고] 파이썬의 오차 해결 방법
# 1. decimal 패키지의 Decimal 함수를 사용해 고정소수점 수로 표현해 처리한다.
# 2. 그래도 순환소수 문제가 발생할 수 있으므로 분수는 fraction 패키지의 Fraction 함수를 사용해 처리한다.
```
```python
# 파이썬 3.6 버전 이상에서 쓸 수 있는 f 형식
반이름 = 'A반'
인원수 = 25
print(f'{반이름}의 총 인원은 {인원수 + 2}명입니까, 아니면 {인원수}명입니까?')
​
자연대수 = 2.7182818284904523536028
​
# 총 자릿수 = 25, 소수점 이하 자릿수 = 15
print(f'자연대수 값이 {자연대수: 20.15f}입니까?') 
```

## 4.2.4 인덱싱(indexing)
- 인덱싱이란 문자열의 특정 부분을 가리키는 일을 말합니다.
```python
# 특정 위치에 있는 문자열 알기 
새_문자열 = "인덱싱이란 문자열의 특정 부분을 가리키는 일을 말합니다."
새_문자열[2]  # 파이썬에서는 0번부터 숫자를 세므로 2는 0, 1, 2의 꼴로 세 번째를 의미한다.
새_문자열[0]  # 그렇다면 0번 문자열은 첫 번째 문자열일 것이다.
새_문자열[29]
새_문자열[-1]  # 거꾸로 세어 올 때는 -1이 뒤에서 첫 번째 문자열에 해당한다.
새_문자열[-3] # 따라서 뒤에서 세 번째 문자열을 -3으로 지정해야 한다.
```

## 4.2.5 슬라이싱(slicing)
- 슬라이싱이란 문자열의 일부를 복제해 내는 일을 말합니다. 
- 원래 있던 문자열은 그대로 있습니다.

```python
새_문자열[0:5]  
# 첫 단어를 따오고 싶다면 그 순서를 지정한다. 
# 여기서 0을 '시작 번호', 5를 '끝 번호'라고 부른다. 
# 5라고 지정한 이유는 끝 번호에  해당하는 문자를 
# 포함하지 않기로 약속되어 있기 때문이다.
```

```python
#결과
'인덱싱이란'
```

```python
새_문자열[0:6] # 빈 칸까지 따올 수 있다.
```

```python
#결과
'인덱싱이란 '
```

```python
새_문자열[:6] # 처음부터 문자열을 따올 때는 시작 번호를 생략한다.
```

```python
#결과
'인덱싱이란 '
```

```python
새_문자열[6:] # 마지막까지 문자열을 따올 때는 끝 번호를 생략한다.
```

```python
#결과
'문자열의 특정 부분을 가리키는 일을 말합니다.'
```

```python
새_문자열[:] # 전부 따올 때는 시작 번호와 끝 번호를 모두 생략한다.
```

```python
#결과
'인덱싱이란 문자열의 특정 부분을 가리키는 일을 말합니다.'
```

```python
새_문자열[6:-6] # 시작 번호나 끝 번호 앞에 음수 표시를 할 수 있다.
```

```python
#결과
'문자열의 특정 부분을 가리키는 일을'
```

```python
새_문자열[-6: -2]
새_문자열[2: 6]
새_문자열[-3: -1]
새_문자열[-3:]

```

```python
#결과
' 말합니'
'싱이란 문자'
'니다'
'니다.'
```

```python
# 슬라이싱 기능을 응용하면 문자열을 다양하게 바꿀 수 있다.
앞_문자열 = 새_문자열[:6] + "..."
뒤_문자열 = 새_문자열[6:]
print(앞_문자열)
print(뒤_문자열)
```
```python
print(새_문자열)
print(새_문자열[6:16]+" ...")
```

## 4.2.6 문자열 (내장) 함수
- 문자열 함수란 문자열 자체에서 제공하는 함수를 말합니다. 
- 문자열.함수명() 꼴로 사용합니다. 이것으로 여러 가지 일을 할 수 있습니다. 
- 이 중에 몇 가지는 자연어 처리를 비롯한 머신러닝/딥러닝에 자주 쓰이는 게 있으니 잘 익혀 둡시다.

```python
# 문자열에 담긴 특정 문자 개수 세기
print(새_문자열.count('을'))
```
```python
# 특정 문자의 위치를 알아내기
print(새_문자열.find('을')) # 을이 처음으로 나온 위치를 알려준다.
print(새_문자열.find('따')) # 찾는 문자열이 없으면 -1을 출력한다.

```
```python
# 특정 문자를 문자열을 이루는 문자들 사이에 끼우기
print(" ".join("python"))
​
print(",".join("python")) 
```
```python
# 결과
p y t h o n
p,y,t,h,o,n
```

```python
# 소문자를 대문자로, 대문자를 소문자로 바꾸기
print("We are the World!".upper())
print("We are the World!".lower())
print("교통 안전 공단 a반에서 수업을 듣던 B라는 사람이 A반에서 ...를 했다고 합니다.".upper())
```
```python
# 왼쪽 빈 칸 지우기
"    제1일차 현황보고    ".lstrip()
# 오른쪽 빈 칸 지우기
"    제1일차 현황보고    ".rstrip()
# 양쪽 빈 칸 지우기
"    제1일차 현황보고    ".strip()
```
```python
# 이쯤에서 다시 '새_문자열' 내용을 출력해 보자.
print(새_문자열)
# 문자열 나누기
print(새_문자열.split())  # 문자열을 리스트(list)라는 데이터 형식으로 바꾼다. 
print("파이썬,학습과,복습을 합니다.".split('과')) # 지정한 구분자를 기준으로 문자열을 나눈다.
```
```python
#결과
['인덱싱이란', '문자열의', '특정', '부분을', '가리키는', '일을', '말합니다.']
['파이썬,학습', ',복습을 합니다.']
```
4.3 리스트(list)
리스트(list)는 '목록'이라는 뜻입니다. 가게에서 물품 목록을 나열해 두듯이 데이터를 나열해 두는 형식입니다. 이런 식으로 어떤 데이터를 담아 두는 형식을 컨테이너(container, 그릇, 용기)'라고 부르는데, 리스트는 컨테이너의 일종입니다.

4.3.1 리스트 형성
# 리스트를 만드는 방법
기수 = ['하나', '둘', '셋', '넷', '다섯'] 
서수 = ['첫째', '둘째', '셋째', '넷째', '다섯째']
수치 = [1, 2, 3, 4, 5]
print(기수)
print(서수)
print(수치)
# 리스트 안에 리스트를 두려면 리스트의 각 원소를 리스트로 만든다.
# 리스트 안에 리스트가 있으면 '중첩 리스트'라고 부른다. 
수_표현 = [[1, '첫째', '하나'], [2, '둘째', '둘'], 3, '셋째', '셋']
print(수_표현)
​
4.3.2 리스트의 인덱싱과 슬라이싱
# 리스트에서도 인덱싱을 할 수 있고 원리는 문자열의 것과 같다.
print(수_표현[0]) # 리스트의 첫 번째 원소가 리스트이므로 부분 리스트가 출력된다.
print(수_표현[0][1]) # 첫 번째 부분 리스트의 두 번째 원소가 출력된다.
print(수_표현[2]) # 리스트의 세 번째 원소는 리스트가 아니므로 값이 그대로 출력된다.
print(수_표현[1][2])
print(수_표현[1])
# 리스트에서도 슬라이싱을 할 수 있고 원리는 문자열의 것과 같다.
print(수_표현[2:]) # 리스트의 세 번째 원소부터 출력한다.
print(수_표현[1:4]) # 리스트의 두 번째 원소부터 네 번째 원소까지 출력한다.
4.3.3 리스트 원소의 수정과 삭제
print(수_표현)
# 리스트 안의 특정 원소를 바꾸거나 지우기
수_표현[0] = [2, '둘째', '둘'] # 첫 번째 원소를 또 다른 리스트로 바꾼다.
수_표현[1] = [3, '셋째', '셋'] # 두 번째 원소를 또 다른 리스트로 바꾼다.
수_표현[2] = [4, '넷째', '넷'] # 네 번째 원소를 또 다른 리스트로 바꾼다. 
​
print(수_표현)
del 수_표현[3: ]  # 네 번째 원소 이후로는 지운다.
                  # del 함수는 파이썬이 기본적으로 제공하는것이다.
                  # del 뒤에 '지울 대상'을 지정하는 꼴이다. 
                  # 지울 대상은 파이썬의 데이터 형식으로 된 객체여야 한다.
print(수_표현)
2.3.4 리스트 연산
# 리스트끼리 더하기
새_수_표현 = [1, '첫째', '하나'] + 수_표현
print(새_수_표현)
# 중첩 리스트 형태로 원소를 추가하기
새_수_표현 = [[1, '첫째', '하나']] + 수_표현 # 추가할 원소를 중첩 리스트 형태로 표현한다.
print(새_수_표현)
# [[교통안전공단, A반], 입니다]
우리반 = [["교통안전공단", "A반"], "입니다"]
print(우리반)
# [[123, 456], [789, 101112]]]
숫자 = [[123, 456], [789, 101112]]
print(숫자)
​
​
# 리스트의 길이 구하기
print(len(수_표현))
print(len(새_수_표현))
print(새_수_표현)
# 리스트를 반복하기
반복_수_표현 = 새_수_표현[0] * 3
print(반복_수_표현)
# 리스트를 중첩된 꼴로 반복되게 하기
중첩_수_표현 = [새_수_표현[0]] * 3  # 반복 대상이 되는 원소를 중첩 리스트 형태로 표현한다.
print(중첩_수_표현)
4.3.5 리스트 (내장) 함수
# 리스트 정렬
print(새_수_표현)
새_수_표현.sort(reverse=True) # reverse를 키워드 파라미터(키워드 인자)라고 부른다.
print(새_수_표현)
새_수_표현.sort() # reverse를 키워드 파라미터(키워드 인자)라고 부른다.
print(새_수_표현)
새_수_표현.sort() # 파라미터를 지정하지 않으면 기본 값이 False이기 때문에 순서대로 정렬된다.
print(새_수_표현)
# 리스트 역정렬
# .sort(reverse=True)와 같은 역할을 하는 함수는 다음과 같다.
새_수_표현.reverse()
print(새_수_표현)
​
문자_정렬 = ["우리", "는", "A반", "입니다", ".", "아니오.", "사실은", "B반이라고", "합니다."]
print(문자_정렬)
문자_정렬.sort()
print(문자_정렬)
문자_정렬.reverse()
print(문자_정렬)
# 리스트 원소 추가
print(새_수_표현)
새_수_표현.append([5, '다섯째', '다섯'])
print(새_수_표현)
# 리스트 원소 삽입
# 자리를 지정해서 원소를 추가할 수 있다. 
새_수_표현.insert(0, [6, '여섯째', '육'])
print(새_수_표현)
새_수_표현.insert(2, [7, '일곱째', '칠'])
print(새_수_표현)
# 다시 정렬해 보자.
새_수_표현.sort()
print(새_수_표현)
4.4 튜플(tuple)
수학 용어로는 짝에 해당합니다. 튜플은 한 번 값을 정해 두면 다시 수정할 수 없다는 점에서 이뮤터블(immutable, 변경 불가) 형식에 해당합니다. 나머지 특징은 리스트와 거의 같습니다.

# 튜플 만들기 
tpl1 = ('튜', '플', '을', '만들기')  # 단순 튜플
tpl2 = ('튜', '플', '들', '을', ('만', '들', '기'))  # 중첩 튜플
tpl3 = (1, 2, 3)  # 원소들이 수치인 튜플 
tpl4 = 4, 5, 6, 7    # 괄호 없이 튜플 생성하기
# 튜플끼리 더하기
print(tpl3 + tpl4)
# 튜플 곱하기
print(tpl3 * 2)
​
# 튜플 길이 알아내기
print(len(tpl1))
print(len(tpl2))
print(len(tpl3))
print(len(tpl4))
4.5 셋(set)
이 데이터 형식은 수학에 나오는 집합(set) 연산을 처리하기 쉽게 하기 위한 것입니다. 인공지능과 관련해서는 데이터 집합(즉, 데이터셋)을 대상으로 여러 가지 연산을 할 때 쓰입니다(주로 고급 기술 수준에서 사용).

4.5.1 셋 형식의 특징
수학에 나오는 집합의 특징과 동일

중복을 허용하지 않음
순서를 고려하지 않음
순서를 꼭 고려해 처리해야 한다면 리스트나 튜플 자료형으로 변환 필요
4.5.2. 셋 형식 데이터 만들기
# 리스트를 셋으로 만들기
A_집합 = set([1, 2, 3, 4, 5])
B_집합 = set([2, 3, 5, 7, 9])
print(A_집합)
print(B_집합)
# 문자열을 셋으로 만들기  
문자_집합 = set("반갑다, 세상아!") # 순서가 사라져 버린다.
print(문자_집합)  
4.5.3 셋 연산
# 연산자를 사용해서 하는 집합 연산
print(A_집합 | B_집합 ) # 합집합
print(A_집합 & B_집합 ) # 교집합
print(A_집합 - B_집합 ) # 차집합
# 함수를 사용해서 하는 집합 연산
print(A_집합.union(B_집합))       # 합집합
print(A_집합.intersection(B_집합))# 교집합
print(A_집합.difference(B_집합))  # 차집합
4.5.4 원소 가감
# 원소 한 개 추가 
A_집합.add(6) 
print(A_집합)
# 원소를 여러 개 추가
A_집합.update([7, 8, 9, 10])
print(A_집합)
# 원소 제거
A_집합.remove(10)
print(A_집합)
4.6 딕셔너리(dictionary)
딕셔너리란 국어 사전에 수록된 모양으로 된 데이터 형식을 말합니다. 즉, 낱말과 뜻풀이 형태로 된 형식을 말하는데, 딕셔너리에서는 이를 각각 키(key)와 값(value)이라고 부릅니다.

4.6.1 딕셔너리 만들기
# 딕셔너리 만들기
# 여기서 '성명'과 '전화번호'가 키이고 나머지가 값이다.
전화부 = {'김갑돌': '010-1234-5678',
          '이을돌': '010-2345-6789'}
print(전화부)
#  또 다른 딕셔너리 만들기
# 딕셔너리의 값으로는 리스트를 지정해도 된다.
성적부 = {'김갑돌': [80, 50, 50],
          '이을돌': [30, 20, 10]} 
print(성적부)
4.6.2 딕셔너리의 기본 기능
# 딕셔너리에 원소 추가하기
# 원소를 '키: 값'이라는 1개 쌍으로 이뤄진 원소를 추가한다.
성적부['박병돌'] = [80, 90, 90]
print(성적부)
성적부['강정돌'] = [50, 60, 70]
# 키로 값을 알아내기
print(성적부['이을돌']) # 딕셔너리에서는 키로만 값을 알 수 있다.
# 딕셔너리 원소 삭제
del 성적부['이을돌']
print(성적부)
4.6.3 딕셔너리 (내장) 함수
# 키만 추출하기 
키_목록 = 성적부.keys()  # 반환되는 객체는 dict_keys다. 
print(키_목록)  
# 키를 리스트 형식으로 추출하기
키_리스트 = list(성적부.keys())
print(키_리스트)
# 값만 추출하기
값_목록 = 성적부.values()
print(값_목록)
# 값을 리스트 형식으로 추출하기
값_리스트 = list(성적부.values())
print(값_리스트)
# 키-값 쌍을 추출하기
키_값_목록 = 성적부.items()
print(키_값_목록)
# 키-값을 리스트 형식으로 추출하기 
키_값_리스트 = list(성적부.items())
print(키_값_리스트)
# 참고: 특정 원소를 뽑아 리스트 형식으로 표현하기
print(list(키_값_리스트[0]))
# 특정 키가 딕셔너리 안에 있는지를 확인하기
print('김갑돌' in 성적부)
print('이을돌' in 성적부)
5. 데이터 형식에 맞게 변수를 형성하기
5.1 튜플 형식에 맞게 변수를 사용하기
# 튜플형 변수 만들기
(t1, t2) = '튜플', '만들기'
# 다음 두 식은 각기 위 식과 똑같은 역할을 한다.
# t1, t2 = '튜플', '만들기'
# t1, t2 = ('튜플', '만들기')
print(t1)
print(t2)
(t1, t2)
# 변수 형식으로 만들면 튜플의 값을 바꿀 수  있다. 
t1 = "새로"
(t1, t2)
t2 = "생성하기"
(t1, t2)
# 변수 형식으로 된 튜플 자체를 저장할 수 있다.
새_튜플 = (t1, t2)
신_튜플 = ("새로운", "튜플")
print(새_튜플)
print(신_튜플)
5.2 리스트 형식에 맞게 변수를 사용하기
이 기능은 머신러닝/딥러닝 처리 시에 자주 쓰입니다. 꼭 외워 둡시다. 튜플 대신에 리스트 형식이라는 점만 다를 뿐 사용법은 앞에서 다룬 방법과 같습니다.

# 리스트의 각 원소를 한 번에 여러 변수에 담기
l1, l2, l3 = [255, 128, 129]  
​
print(l1)
print(l2)
print(l3)
print([l1, l2, l3])
# 개별 원소를 변수들로 이뤄진 리스트에 담기
[원소1, 원소2, 원소3] = 128, 155, 12
​
print(원소1)
print(원소2)
print(원소3)
새_리스트 = [원소3, 원소1, 원소2]
print(새_리스트)
5.3 그 밖의 변수 사용법
# 변수 값을 치환하기
변수1 = 10
변수2 = 20
변수1, 변수2 = 변수2, 변수1
print(변수1, 변수2)
a = 10
b = 20
c = 30
​
# 어떤 코드?
a, b, c = c, b, a
​
print(a, b, c)
# 여러 변수에 같은 값을 대입하기 
갑 = 을 = 병 = 정 = '신입사원'
print(갑, 을, 병, 정)
a = b = c = d = 10

print(a + b + c + d)
a = b = c = d = 10
​
print(a + b + c + d)